[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra
markers =
    p0: ETL critical guard tests (blocking)
    suite_etl: ETL pipeline tests
    suite_legacy: Legacy pipeline and compatibility tests
    suite_fastmcp: FastMCP tooling tests
